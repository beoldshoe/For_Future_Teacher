spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/for_teacher?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: future_teacher
    password: future_teacher # ?? ????? ????? ???!

  jpa:
    hibernate:
      ddl-auto: create # ?? ????? ???? ??? ??
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect # MySQL 8.x ?? ?
        format_sql: true
